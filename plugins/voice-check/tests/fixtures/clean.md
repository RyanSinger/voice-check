# Release notes

The plan is simple. Ship the MVP this week. Iterate from real usage next week.

We reviewed the numbers on Monday and chose the smallest change that moves the metric. No extra scope. No launch copy.

## Rollout checklist

- First top level item
- Second item with detail
  - Nested bullet one
  - Nested bullet two
    - Even deeper nested bullet
- Third top level item

## Commands used

```bash
ls -la
grep -r "foo" .
rm -rf build/
echo "ship - and - iterate"
```

Inline code like `foo-bar` or `ls -la` should not trip the scanner.

## Results

| Step | Owner | Status |
| ---- | ----- | ------ |
| Plan | Alex  | done   |
| Ship | Sam   | done   |
| Log  | Pat   | done   |

We finished the work. The team hit the Friday target with time to spare. Next sprint picks up from the same branch.
